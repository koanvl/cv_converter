<div class="min-h-screen bg-gray-50 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="md:flex md:items-center md:justify-between mb-8">
      <div class="min-w-0 flex-1">
        <h1 class="text-3xl font-bold leading-7 text-gray-900 sm:truncate sm:text-4xl">
          📄 Мои шаблоны
        </h1>
        <p class="mt-2 text-sm text-gray-500">
          Создавайте и редактируйте шаблоны с помощью GrapesJS
        </p>
      </div>
      <div class="mt-4 flex md:ml-4 md:mt-0">
        <%= link_to new_template_path, 
            class: "inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-md shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
          ➕ Создать новый шаблон
        <% end %>
      </div>
    </div>

    <% if @templates.any? %>
      <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
        <% @templates.each do |template| %>
          <div class="bg-white overflow-hidden shadow rounded-lg hover:shadow-lg transition-shadow duration-200">
            <div class="p-6">
              <div class="flex items-center justify-between">
                <h3 class="text-lg font-medium text-gray-900 truncate">
                  <%= template.title %>
                </h3>
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                  Активен
                </span>
              </div>
              
              <p class="mt-2 text-sm text-gray-500">
                Создан: <%= template.created_at.strftime("%d.%m.%Y в %H:%M") %>
              </p>
              
              <% if template.updated_at != template.created_at %>
                <p class="text-sm text-gray-500">
                  Обновлен: <%= template.updated_at.strftime("%d.%m.%Y в %H:%M") %>
                </p>
              <% end %>
              
              <div class="mt-4 flex space-x-3">
                <%= link_to template_path(template),
                    class: "flex-1 inline-flex justify-center items-center px-3 py-2 text-sm font-medium text-gray-700 bg-gray-100 border border-transparent rounded-md hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" do %>
                  👁️ Просмотр
                <% end %>
                
                <%= link_to edit_template_path(template),
                    class: "flex-1 inline-flex justify-center items-center px-3 py-2 text-sm font-medium text-blue-700 bg-blue-100 border border-transparent rounded-md hover:bg-blue-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
                  ✏️ Редактировать
                <% end %>
                
                <%= link_to template_path(template),
                    method: :delete,
                    confirm: "Вы уверены, что хотите удалить шаблон '#{template.title}'?",
                    class: "inline-flex items-center px-3 py-2 text-sm font-medium text-red-700 bg-red-100 border border-transparent rounded-md hover:bg-red-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500" do %>
                  🗑️ Удалить
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-12">
        <div class="mx-auto h-24 w-24 text-gray-400 mb-4">
          📄
        </div>
        <h3 class="text-lg font-medium text-gray-900 mb-2">
          Пока нет шаблонов
        </h3>
        <p class="text-gray-500 mb-6">
          Создайте свой первый шаблон с помощью GrapesJS
        </p>
        <%= link_to new_template_path,
            class: "inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-md shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
          ➕ Создать первый шаблон
        <% end %>
      </div>
    <% end %>
  </div>
</div>