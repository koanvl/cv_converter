# Руководство по использованию CV Converter

## Обзор системы

CV Converter - это система для автоматической генерации резюме на основе шаблонов и JSON данных. Система состоит из трех основных частей:

1. **Examples** - Создание шаблонов резюме с переменными
2. **Генератор резюме** - Управление кандидатами и генерация резюме
3. **Templates** - Система шаблонов GrapesJS (существующая)

## Пошаговое использование

### 1. Создание шаблона резюме

1. Перейдите в раздел **Examples**
2. Нажмите **"New Example"**
3. Заполните:
   - **Title**: Название шаблона (например, "Шаблон для Python разработчика")
   - **Format**: Выберите формат (modern, classic, default)
   - **Published**: Отметьте для активации
4. В редакторе создайте структуру резюме
5. Используйте кнопку **"Вставить переменную"** (📄) для добавления динамических полей
6. Доступные переменные:
   - `{{name}}` - Имя кандидата
   - `{{current_role}}` - Текущая роль
   - `{{projects[0].title}}` - Название первого проекта
   - `{{projects[0].overview}}` - Описание проекта
   - `{{skills_qualifications[0].description}}` - Описание навыков
   - И многие другие...

### 2. Добавление кандидата

1. Перейдите в **"Генератор резюме"**
2. Нажмите **"Добавить кандидата"**
3. Заполните:
   - **Имя кандидата**: ФИО
   - **Исходное резюме**: Оригинальный текст (опционально)
   - **JSON данные**: Структурированные данные кандидата
4. Используйте кнопку **"Загрузить пример"** для получения образца JSON
5. Сохраните кандидата

### 3. Генерация резюме

1. В разделе **"Генератор резюме"** выберите кандидата
2. На странице кандидата выберите нужный шаблон
3. Нажмите **"Сгенерировать"**
4. Просмотрите результат в окне предпросмотра
5. При необходимости отредактируйте данные кандидата или шаблон

## Структура JSON данных

```json
{
  "name": "Ivan B.",
  "current_role": "Python developer",
  "projects": [
    {
      "title": "Project 1",
      "project_name": "NDA",
      "overview": "Описание проекта...",
      "technologies": "Python, FastAPI, Docker",
      "role": "Python Developer",
      "common_tasks": "• Задача 1\n• Задача 2",
      "duration": "9 months"
    }
  ],
  "skills_qualifications": [
    {
      "title": "Programming Languages",
      "description": "Python, JavaScript"
    }
  ],
  "languages": [
    {
      "title": "English",
      "level": "Upper-Intermediate"
    }
  ]
}
```

## Доступные переменные

### Основная информация
- `{{name}}` - Имя
- `{{current_role}}` - Текущая роль

### Проекты
- `{{projects[0].title}}` - Название проекта
- `{{projects[0].project_name}}` - Имя проекта
- `{{projects[0].overview}}` - Описание
- `{{projects[0].technologies}}` - Технологии
- `{{projects[0].role}}` - Роль в проекте
- `{{projects[0].common_tasks}}` - Основные задачи
- `{{projects[0].duration}}` - Длительность

### Навыки
- `{{skills_qualifications[0].title}}` - Название навыка
- `{{skills_qualifications[0].description}}` - Описание

### Языки
- `{{languages[0].title}}` - Название языка
- `{{languages[0].level}}` - Уровень

## Советы по использованию

1. **Создавайте переиспользуемые шаблоны** - один шаблон может использоваться для множества кандидатов
2. **Используйте таблицы для структурированных данных** - кнопка "Insert Table" в редакторе
3. **Тестируйте шаблоны** - создайте тестового кандидата для проверки
4. **Сохраняйте исходные резюме** - для справки и анализа
5. **Используйте CSS для стилизации** - добавляйте собственные стили в поле "Custom CSS"

## Устранение неполадок

### Переменная не заменяется
- Проверьте правильность написания переменной
- Убедитесь, что данные есть в JSON кандидата
- Проверьте структуру JSON (массивы должны иметь элементы)

### Ошибка при генерации
- Проверьте валидность JSON данных
- Убедитесь, что шаблон содержит корректный HTML
- Проверьте консоль браузера на наличие ошибок JavaScript

### Стили не применяются
- Проверьте CSS синтаксис
- Убедитесь, что селекторы корректны
- Используйте инструменты разработчика браузера для отладки

## Расширение системы

В будущем планируется добавить:
- AI обработку исходных резюме
- Экспорт в PDF/DOCX
- Больше типов переменных
- Условная логика в шаблонах
- Массовая генерация резюме
